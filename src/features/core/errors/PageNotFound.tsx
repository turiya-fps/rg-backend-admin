import type { FunctionComponent } from 'react';

/**
 * A page that is rendered in cases where the router encounters 404 not found.
 */
export const PageNotFound: FunctionComponent = () => {
  return (
    <h1>Page not found</h1>
  );
};
