import type { FunctionComponent } from 'react';
import { Heading } from '../../../../components/text/Heading';

export const Dashboard: FunctionComponent = () => {
  return (
    <Heading type={'h1'} label={'Dashboard'} />
  );
};
